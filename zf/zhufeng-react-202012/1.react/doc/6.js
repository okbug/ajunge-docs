import { REACT_TEXT } from "../src/constants"

let element = {
    type:'p',
    props:{
        children:2
    }
}

let element = {
    type:'p',
    props:{
        children:{type:REACT_TEXT,content:2}
    }
}