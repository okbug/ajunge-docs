export default {
    height: {
        type: String, // 200px
        default: "200px",
    },
    autoplay: {
        type: Boolean,
        default: true,
    },
    delay: {
        type: Number,
        default: 3000,
    },
    initialIndex: {
        type: Number,
        default: 0,
    },
    loop: {
        type: Boolean,
        default: true,
    },
}